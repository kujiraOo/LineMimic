-- Table for sprite sheet and textures for the level editor

return {

	stepperSheet = graphics.newImageSheet( "gfx/stepperSheet.png", {

		width = 48,
		height = 24,
		numFrames = 5,
		sheetContentWidth = 240,
		sheetContentHeight = 24,
	}),
}